{
  "name": "Qubinest-Backend",
  "version": "1.0.0",
  "engines": {
    "node": ">=16.0.0"
  },
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js",
    "start": "node --version && node server.js",
    "build": "npm install && npx prisma generate",
    "clean": "rm -rf node_modules dist package-lock.json && npm cache clean --force",
    "setup": "npm run clean && npm install && npx prisma generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "5.15.0",
    "bcrypt": "^5.1.1",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.18.1",
    "html-pdf": "^3.0.1",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "nodemailer": "^6.9.16"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "prisma": "5.15.0"
  }
}
